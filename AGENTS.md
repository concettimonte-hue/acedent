# ACEDENT 프로젝트 규칙

## 배포

- 배포 대상은 Cloudflare Pages 입니다. Vercel 이 아닙니다.
- main 브랜치에 push 하면 자동 배포됩니다.
- Vercel 에 새로 배포하거나 설정을 추가하지 마세요.
- vercel.json 등 기존 Vercel 파일은 롤백 대비용으로 유지하되
  갱신하지 마세요.

## 기술 제약

- Cloudflare Pages 호환되지 않는 기능 사용 금지
  (Node 전용 API, 서버 전용 런타임 기능 등)
- 새 라이브러리 추가 전 Cloudflare 호환 여부를 먼저 확인
- 이미지는 사전 압축 방식 사용 (런타임 최적화 사용 안 함)

## 명령어

- 빌드: npm run build
- 타입 체크: npx tsc --noEmit
- 작업 완료 전 위 두 가지를 반드시 통과시킬 것

## 디자인 원칙

- 검정 배경 + 노랑 포인트 유지
- 기존 컴포넌트/문구/구조를 임의로 변경하지 말 것
- 새 디자인 언어 도입 금지

## 이미지 업로드 규칙

- 가로 1600px 이하, 200KB 이하
- 전/후 사진은 동일한 각도로 촬영

## 보안

- .env.local 은 절대 커밋하지 말 것
- API 토큰을 코드나 설정 파일에 하드코딩 금지

## 금지 사항

- 사전 협의 없이 파일 삭제 금지
- 미추적 파일을 임의로 커밋하지 말 것
